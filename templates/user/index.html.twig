{% extends 'base.html.twig' %}

{% block body %}
<table>
    <thead>
        <tr>
            <th>Username</th>
        </tr>
    </thead>
    <tbody>
        {% for user in users %}
            <tr>
                <td>{{ user.username }}</td>
            </tr>
        {% endfor %}
    </tbody>
</table>

<a href="{{ path('user_create') }}">create</a>
{% endblock %}
