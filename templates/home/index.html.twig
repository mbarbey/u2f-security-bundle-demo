{% extends 'base.html.twig' %}


{% block body %}
<div class="row mb-2">
    <div class="col-md-6">
        <a href="{{ path('login') }}" class="btn btn-primary btn-lg btn-block">Login</a>
    </div>
    <div class="col-md-6">
        <a href="{{ path('register') }}" class="btn btn-primary btn-lg btn-block">Register</a>
    </div>
</div>

{% endblock %}
